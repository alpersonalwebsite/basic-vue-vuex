# Copy to .env and edit. .env is gitignored; only VUE_APP_-prefixed variables reach
# client code, and they are inlined into the bundle at build time, so never put a
# secret here.
#
# Defaults to the local API from alpersonalwebsite/node-express-postgresql.
VUE_APP_API_URL=http://localhost:3333/api/users
